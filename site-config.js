window.WRL_SITE_CONFIG = {
  playtest: {
    // Uncomment and fill in when you have a packaged build ready to share.
    // Cloudflare R2 is recommended for hosting large Unreal builds.
    //
    // url:      'https://your-r2-url/wrl-movement-0.1.zip',
    // version:  'Movement 0.1',
    // platform: 'Windows 10 / 11',
    // size:     '420 MB',
    // note:     'Extract the ZIP and run WRL.exe — no installer required.',
  }
};
